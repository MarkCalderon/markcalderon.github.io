# Project Rules Pointer

This repository's active AI coding rules are maintained in:

- `.cursor/rules/standard-baseline.mdc`

`RULES.md` is a human-readable pointer only. Update the `.mdc` rule file as the source of truth.
